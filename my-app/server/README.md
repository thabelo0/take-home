# Student Management - Backend

## Setup
1. Copy `.env.example` to `.env` and fill your MySQL credentials.
2. Install dependencies:
   ```
   npm install
   ```
3. Run:
   ```
   npm run dev
   ```

The server listens on the port defined in `.env` or 4000 by default.
